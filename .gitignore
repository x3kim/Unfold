# ===================================================================
# General
# ===================================================================

# Log files
# These files are generated by npm or other tools for debugging purposes.
npm-debug.log*
yarn-error.log
yarn-debug.log
lerna-debug.log

# Operating System generated files
# These are metadata files created by macOS, Windows, etc.
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ===================================================================
# Node.js / npm
# ===================================================================

# Dependencies
# This is the most important one! The node_modules folder can be huge
# and should always be re-generated by running `npm install`.
node_modules/

# Lock-files from other package managers (if you don't use them)
# package-lock.json is usually checked in, but if you switch, you might
# want to ignore the old one. For now, we keep it.
# yarn.lock
# pnpm-lock.yaml

# ===================================================================
# Electron / Build Artifacts
# ===================================================================

# Build output
# The `dist` folder is created by `electron-builder` and contains the final
# packaged application. It should never be committed to Git.
dist/
build/
release/

# Electron Forge specific files (good to have just in case)
.electron/
out/

# ===================================================================
# Project Specific
# ===================================================================

# Unfold App's own output folder
# If you run the app on its own source code folder for testing,
# this prevents the output `..._unfolded` folder from being committed.
*_unfolded/

# ===================================================================
# Editor & IDE specific
# ===================================================================

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/

# JetBrains (WebStorm, etc.)
.idea/

# Sublime Text
*.sublime-project
*.sublime-workspace